export const JWT_REFRESH_TOKEN_SECRET = 'jwt.refreshToken.secret';

export const JWT_REFRESH_TOKEN_EXPIRATION_TIME = 'jwt.refreshToken.expirationTime';

export const JWT_ACCESS_TOKEN_SECRET = 'jwt.accessToken.secret';

export const JWT_ACCESS_TOKEN_EXPIRATION_TIME = 'jwt.accessToken.expirationTime';
