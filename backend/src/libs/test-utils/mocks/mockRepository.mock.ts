// import { MongoGenericRepository } from 'src/libs/repositories/mongo/mongo-generic.repository';

// export type MockRepository<T> = Partial<Record<keyof MongoGenericRepository<T>, jest.Mock>>;

// export default <T>(): MockRepository<T> => ({
// 	find: jest.fn(),
// 	findBy: jest.fn(),
// 	findOne: jest.fn(),
// 	remove: jest.fn(),
// 	delete: jest.fn(),
// 	count: jest.fn(),
// 	countBy: jest.fn()
// });
