import { styled } from '@/styles/stitches/stitches.config';

const StyledList = styled('ul', {
  display: 'flex',
  alignItems: 'center',
  gap: 5,

  margin: 0,
  padding: 0,

  listStyle: 'none',
});

export { StyledList };
