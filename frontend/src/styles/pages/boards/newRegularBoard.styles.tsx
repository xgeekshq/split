import { styled } from '@/styles/stitches/stitches.config';

const ContentSelectContainer = styled('section', {
  display: 'flex',
  width: '100%',
  minHeight: 'calc(100vh - $sizes$92)',
  alignItems: 'center',
  justifyContent: 'center',
});

export { ContentSelectContainer };
